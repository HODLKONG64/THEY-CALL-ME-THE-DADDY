name: main_with_openai_upgrade_gate

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "main_with_openai_upgrade_gate is deprecated."
          echo "Use .github/workflows/cycle.yml as the single canonical workflow."
