name: daddy-cycle

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "daddy-cycle is deprecated."
          echo "Use .github/workflows/cycle.yml as the single canonical workflow."
