name: pre-update-guard

on:
  workflow_dispatch:

jobs:
  deprecated-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "pre_update_guard.yml is disabled to avoid duplicate push-trigger workflows."
          echo "Use .github/workflows/cycle.yml as the single canonical workflow."
